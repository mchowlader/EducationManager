namespace EduManager.Domain.Enums;

public enum AdminRole
{
    Owner = 1,       // Full system control
    SuperAdmin = 2,  // High-level administrative permissions
    Support = 3      // Read-only access
}